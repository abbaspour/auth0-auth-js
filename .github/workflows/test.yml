name: Build and Test

on:
  merge_group:
  workflow_dispatch:
  pull_request:
    branches: [main, early-access]
  push:
    branches: [main, early-access]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  CACHE_KEY: "${{ github.ref }}-${{ github.run_id }}-${{ github.run_attempt }}"

jobs:
  test:
    strategy:
      matrix:
        node: [ 20, 22 ]
    name: Build Package
    runs-on:
      labels: ubuntu-22.04-2cpu-8ram-75ssd

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js with npm caching
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: "npm"

      - name: Install dependencies
        run: npm install

      - name: Build @auth0/auth-sdk
        run: npm run build -w @auth0/auth-sdk

      - name: Build @auth0/server-sdks
        run: npm run build -w @auth0/server-sdk

      - name: Build @auth0/api-sdk
        run: npm run build -w @auth0/api-sdk

      - name: Build @auth0/fastify-web
        run: npm run build -w @auth0/fastify-web

      - name: Build @auth0/fastify-api
        run: npm run build -w @auth0/fastify-api

      - name: Test @auth0/auth-sdk
        run: npm run test:ci -w @auth0/auth-sdk

      - name: Test @auth0/server-sdk
        run: npm run test:ci -w @auth0/server-sdk
      
      - name: Test @auth0/api-sdk
        run: npm run test:ci -w @auth0/api-sdk

      - name: Test @auth0/fastify-web
        run: npm run test:ci -w @auth0/fastify-web

      - name: Test @auth0/fastify-api
        run: npm run test:ci -w @auth0/fastify-api

  lint:
    name: Lint
    runs-on:
      labels: ubuntu-22.04-2cpu-8ram-75ssd

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js with npm caching
        uses: actions/setup-node@v4
        with:
          node-version: 22
    
      - name: Install dependencies
        run: npm install

      - name: Run @auth0/auth-sdk
        run: npm run lint -w @auth0/auth-sdk

      - name: Run @auth0/server-sdk
        run: npm run lint -w @auth0/server-sdk
  
      - name: Lint @auth0/api-sdk
        run: npm run lint -w @auth0/api-sdk
      
      - name: Lint @auth0/fastify-web
        run: npm run lint -w @auth0/fastify-web

      - name: Lint @auth0/fastify-api
        run: npm run lint -w @auth0/fastify-api